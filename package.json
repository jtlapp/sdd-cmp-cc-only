{
  "name": "collaborative-taxon-tree-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Server for shared, multi-owner trees of taxa with proposal-based collaboration.",
  "scripts": {
    "build": "tsc",
    "start": "node dist/src/server.js",
    "test": "tsc && node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  }
}
